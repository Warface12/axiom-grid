import Link from "next/link";import {ShieldAlert,ArrowLeft} from "lucide-react";import {buildMetadata} from "@/lib/seo";
export const metadata=buildMetadata({title:"Partner link unavailable — TopPick.pro",description:"This commercial link is not currently available for the detected market.",path:"/unavailable",noIndex:true});
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const p=await searchParams;return <main className="shell content-shell"><section className="tp-state-card"><ShieldAlert/><span>COMPLIANCE GATE</span><h1>Partner link unavailable.</h1><p>TopPick.pro keeps promotional links disabled when market eligibility is unknown, expired or restricted. You can still use the public research profile and verify availability directly with the provider.</p>{p.market&&<small>Detected market: {p.market}</small>}<Link href="/compare"><ArrowLeft/>Back to comparisons</Link></section></main>}
