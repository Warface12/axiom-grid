import{notFound}from"next/navigation";import Link from"next/link";import{Globe2,ShieldCheck,ExternalLink}from"lucide-react";import{MARKET_POLICIES,getMarketPolicy}from"@/lib/markets";import{buildMetadata}from"@/lib/seo";
export function generateStaticParams(){return MARKET_POLICIES.map(m=>({code:m.code.toLowerCase()}))}
export async function generateMetadata({params}:{params:Promise<{code:string}>}){const{code}=await params;const m=getMarketPolicy(code);return m?buildMetadata({title:`${m.name} platform research — TopPick.pro`,description:`Jurisdiction-aware broker, exchange and wallet research framework for ${m.name}.`,path:`/markets/${code.toLowerCase()}`}):{}}
export default async function Page({params}:{params:Promise<{code:string}>}){const{code}=await params;const m=getMarketPolicy(code);if(!m)notFound();return <main className="shell content-shell"><section className="page-hero"><span>{m.code} / MARKET RESEARCH</span><h1>{m.name}</h1><p>{m.notes}</p></section><section className="tp-market-policy"><div><Globe2/><h2>Research availability</h2><p>Public research can exist independently of commercial promotion.</p><b>{m.publicResearch?"Research pages enabled":"Research pages withheld"}</b></div><div><ShieldCheck/><h2>Commercial default</h2><p>Partner links require an explicit, current market record backed by evidence.</p><b>Blocked until approved</b></div></section><Link className="primary-btn" href="/compare">Compare research <ExternalLink size={15}/></Link></main>}
