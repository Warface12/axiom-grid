import { Globe2, ShieldCheck, Radar } from "lucide-react";
import { buildMetadata } from "@/lib/seo";
export const metadata=buildMetadata({title:"Market Access — Axiom Grid",description:"Jurisdiction-aware research for exchange, broker and wallet availability.",path:"/markets"});
const markets=[{name:"United Kingdom",code:"GB",note:"Research queue: exchange, broker and wallet availability is evaluated separately."},{name:"European Economic Area",code:"EEA",note:"Entity, product and promotion rules can differ by member state."},{name:"Canada",code:"CA",note:"Provincial rules may affect platform availability and marketing eligibility."}];
export default function Page(){return <main className="shell content-shell"><section className="page-hero"><span>MARKET ACCESS</span><h1>Jurisdiction is part of the product.</h1><p>Availability, product scope and commercial eligibility are tracked independently instead of assumed globally.</p></section><div className="market-grid">{markets.map(m=><article className="market-card" key={m.code}><Globe2/><small>{m.code}</small><h2>{m.name}</h2><p>{m.note}</p></article>)}</div><section className="ag-system-map"><div className="sys-row"><span>01</span><b><ShieldCheck/> Availability</b><em>Can users access the product?</em></div><div className="sys-row"><span>02</span><b><Radar/> Promotion</b><em>Can it be commercially promoted?</em></div></section></main>}
