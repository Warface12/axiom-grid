import Link from "next/link";import { platforms,guides } from "@/lib/demo-data";import { buildMetadata } from "@/lib/seo";export const metadata=buildMetadata({title:"Search",description:"Search platform and guide research.",path:"/search",noIndex:true});export default function Page(){return <main><section className="shell page-hero"><span>SEARCH</span><h1>Research index</h1><p>Static launch index. Add server search when the database is connected.</p></section><section className="shell content-shell"><div className="prose-card"><h2>Platforms</h2>{platforms.map(p=><p key={p.id}><Link href={`/${p.kind}s/${p.slug}`}><b>{p.name}</b> — {p.short}</Link></p>)}<h2>Guides</h2>{guides.map(g=><p key={g.slug}><Link href={`/learn/${g.slug}`}><b>{g.title}</b></Link></p>)}</div></section></main>}
