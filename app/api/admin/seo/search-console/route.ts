import { NextResponse } from "next/server";import { isAdminUser } from "@/lib/supabase/admin";import { getSearchConsoleConfigStatus,getSearchConsoleOpportunities,syncSearchConsoleMetrics } from "@/lib/searchConsole";import { getSeoRecommendations,runSeoCycle } from "@/lib/seoEngine";
export const runtime="nodejs";export const dynamic="force-dynamic";
export async function GET(){if(!(await isAdminUser()))return NextResponse.json({ok:false,error:"Unauthorized"},{status:401});return NextResponse.json({ok:true,status:getSearchConsoleConfigStatus(),opportunities:await getSearchConsoleOpportunities(),recommendations:await getSeoRecommendations()})}
export async function POST(){if(!(await isAdminUser()))return NextResponse.json({ok:false,error:"Unauthorized"},{status:401});try{await syncSearchConsoleMetrics();return NextResponse.json(await runSeoCycle({syncGsc:false}))}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:"Search Console sync failed."},{status:500})}}
