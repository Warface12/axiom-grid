import { notFound } from "next/navigation";import { getPlatform } from "@/lib/demo-data";import { PlatformDetail } from "@/components/PlatformDetail";import { buildMetadata, breadcrumbJsonLd } from "@/lib/seo";import { SITE_URL } from "@/lib/site";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPlatform(slug);if(!p)return buildMetadata({title:"Platform not found",description:"The requested research profile is unavailable.",path:"/wallets/"+slug,noIndex:true});return buildMetadata({title:p.name+" Review & Research Profile",description:p.description,path:"/wallets/"+p.slug});}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getPlatform(slug);if(!p||p.kind!=="wallet")notFound();const bc=breadcrumbJsonLd([{name:"Home",url:SITE_URL},{name:"Wallets",url:SITE_URL+"/wallets"},{name:p.name,url:SITE_URL+"/wallets/"+p.slug}]);return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(bc)}}/><section className="shell page-hero"><span>WALLETS / PROFILE</span><p>Evidence-first platform research</p></section><PlatformDetail platform={p}/></main>}
