import { platforms } from "@/lib/demo-data";import { PlatformIndex } from "@/components/PlatformIndex";import { buildMetadata } from "@/lib/seo";
export const metadata=buildMetadata({title:"Crypto Wallet Reviews & Comparisons",description:"Compare hardware and software crypto wallets by custody model, recovery, security, supported assets and practical usability.",path:"/wallets"});
export default function Page(){return <main><section className="shell page-hero"><span>PLATFORM HUB / WALLETS</span><h1>Crypto wallets</h1><p>Compare custody models, recovery methods and wallet formats with a clear distinction between exchange accounts and self-custody products.</p></section><section className="shell content-shell"><PlatformIndex kind="wallet" items={platforms}/></section></main>}
