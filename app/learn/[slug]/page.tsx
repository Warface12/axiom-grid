import { notFound } from "next/navigation";import { guides } from "@/lib/demo-data";import { buildMetadata } from "@/lib/seo";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=guides.find(x=>x.slug===slug);return buildMetadata({title:g?.title||"Guide",description:g?.excerpt||"Crypto education guide.",path:`/learn/${slug}`,noIndex:!g});}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const g=guides.find(x=>x.slug===slug);if(!g)notFound();return <main><section className="shell page-hero"><span>{g.category.toUpperCase()} / {g.readTime}</span><h1>{g.title}</h1><p>{g.excerpt}</p></section><section className="shell content-shell"><article className="prose-card"><h2>Editorial draft framework</h2><p>This page is scaffolded for an original, source-backed article. Before publication, expand it with primary sources, concrete examples, current market details where relevant, and a reviewed update date.</p><h2>What the final guide should answer</h2><p>Define the product clearly, explain the decision factors, separate factual claims from opinion, show major risks, link to relevant comparison hubs, and cite official sources for time-sensitive product or regulatory information.</p><h2>SEO intent</h2><p>The guide is designed as supporting informational content that internally links toward higher-intent comparison and review pages without turning education into a disguised sales pitch.</p></article></section></main>}
