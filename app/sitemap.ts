import type{MetadataRoute}from"next";import{SITE_URL}from"@/lib/site";import{getSitemapPlatforms}from"@/lib/platforms";import{MARKET_POLICIES}from"@/lib/markets";
const STATIC=["/","/exchanges","/brokers","/wallets","/research","/updates","/compare","/learn","/markets","/fees","/security","/how-we-rate","/editorial-policy","/corrections","/contact","/legal/affiliate-disclosure","/legal/risk-disclosure","/legal/privacy","/legal/terms"];
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const now=new Date();const base=STATIC.map(path=>({url:`${SITE_URL}${path}`,lastModified:now,changeFrequency:path==="/"?"daily" as const:"weekly" as const,priority:path==="/"?1:.7}));const markets=MARKET_POLICIES.filter(m=>m.publicResearch).map(m=>({url:`${SITE_URL}/markets/${m.code.toLowerCase()}`,lastModified:now,changeFrequency:"monthly" as const,priority:.62}));const platforms=await getSitemapPlatforms();const dynamic=platforms.map(p=>({url:`${SITE_URL}/${p.kind}s/${p.slug}`,lastModified:new Date(p.updatedAt==="Recently"?Date.now():p.updatedAt),changeFrequency:"weekly" as const,priority:p.featured?.85:.72}));return[...base,...markets,...dynamic]}
