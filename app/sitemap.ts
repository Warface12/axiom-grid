import type { MetadataRoute } from "next";import { SITE_URL } from "@/lib/site";import { guides,platforms } from "@/lib/demo-data";
export default function sitemap():MetadataRoute.Sitemap{const now=new Date();const staticRoutes=["","/exchanges","/brokers","/wallets","/compare","/learn","/markets","/how-we-rate","/editorial-policy","/about","/legal/affiliate-disclosure","/legal/privacy","/legal/terms"];const routes=[...staticRoutes,...guides.map(g=>`/learn/${g.slug}`),...platforms.map(p=>`/${p.kind}s/${p.slug}`)];return routes.map(route=>({url:`${SITE_URL}${route}`,lastModified:now,changeFrequency:route===""?"daily":"weekly",priority:route===""?1:route.split("/").filter(Boolean).length===1?.85:.65}))}
