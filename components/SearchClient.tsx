"use client";import {useEffect,useState}from"react";import Link from"next/link";import{Search,ArrowUpRight,LoaderCircle}from"lucide-react";
type Item={id:string;slug:string;name:string;kind:string;short:string;logoText:string};
export function SearchClient(){const[q,setQ]=useState("");const[items,setItems]=useState<Item[]>([]);const[loading,setLoading]=useState(false);useEffect(()=>{const t=setTimeout(async()=>{if(q.trim().length<2){setItems([]);return}setLoading(true);try{const r=await fetch(`/api/search?q=${encodeURIComponent(q)}`);const j=await r.json();setItems(j.items||[])}finally{setLoading(false)}},250);return()=>clearTimeout(t)},[q]);return <section className="tp-search-console"><div className="tp-search-box"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search exchanges, brokers, wallets…" aria-label="Search platforms"/>{loading&&<LoaderCircle className="spin"/>}</div>{q.trim().length>1&&<div className="tp-search-results">{!loading&&!items.length?<p>No public research profiles matched that search.</p>:items.map(i=><Link key={i.id} href={`/${i.kind}s/${i.slug}`}><span>{i.logoText}</span><div><b>{i.name}</b><small>{i.kind} · {i.short}</small></div><ArrowUpRight/></Link>)}</div>}</section>}
