import Link from"next/link";import{ArrowUpRight,Newspaper}from"lucide-react";import{getPublicPartnerPosts}from"@/lib/partnerPosts";
export async function LatestPartnerUpdates(){const posts=await getPublicPartnerPosts(3);if(!posts.length)return null;return <section className="ag-partner-zone tp-latest-updates"><div className="ag-section-marker">TOPPICK / SOURCED UPDATES</div><div className="ag-partner-head"><div><h2>Recent platform updates</h2><p>Factual, source-linked changes shown only where the related platform has approved market availability.</p></div><Link href="/updates">All updates <ArrowUpRight/></Link></div><div className="tp-update-grid compact">{posts.map(p=><Link href={`/updates/${p.slug}`}className="tp-update-card"key={p.id}><div className="tp-update-meta"><Newspaper/><span>{p.postType.replaceAll("_"," ")}</span></div><h3>{p.title}</h3><p>{p.excerpt}</p></Link>)}</div></section>}
