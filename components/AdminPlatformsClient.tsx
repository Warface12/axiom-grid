"use client";
import { useEffect,useMemo,useState } from "react";
import { ExternalLink,Plus,RefreshCw,Search,Trash2,X,WandSparkles } from "lucide-react";
type Row={id:string;slug:string;name:string;kind:"exchange"|"broker"|"wallet";status:"research"|"verified"|"restricted";official_url?:string|null;affiliate_url?:string|null;affiliate_partner_id?:string|null;short_description?:string|null;logo_url?:string|null;tags?:string[]|null;pros?:string[]|null;cons?:string[]|null;featured?:boolean;visible?:boolean;updated_at?:string|null;[key:string]:any};
const blank={name:"",slug:"",kind:"broker",status:"research",official_url:"",affiliate_url:"",affiliate_partner_id:"",short_description:"",full_review:"",logo_url:"",custody_model:"",tags:"",fee_summary:"",security_summary:"",regulatory_summary:"",product_summary:"",pros:"",cons:"",seo_title:"",seo_description:"",featured:false,visible:false};
function slugify(v:string){return v.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"")}
function inferName(value:string){try{const host=new URL(value).hostname.replace(/^www\./,"");const root=host.split(".")[0]||"";return root?root.charAt(0).toUpperCase()+root.slice(1):""}catch{return""}}
export function AdminPlatformsClient(){
 const[rows,setRows]=useState<Row[]>([]),[loading,setLoading]=useState(true),[message,setMessage]=useState(""),[query,setQuery]=useState(""),[open,setOpen]=useState(false),[more,setMore]=useState(false),[form,setForm]=useState<any>(blank);
 async function load(){setLoading(true);setMessage("");try{const r=await fetch("/api/admin/platforms",{cache:"no-store"});const j=await r.json();setRows(j.items||[]);if(!j.ok)setMessage(j.error||"Could not load platforms.")}catch{setMessage("Could not connect to the admin data service.")}finally{setLoading(false)}}
 useEffect(()=>{load()},[]);const filtered=useMemo(()=>rows.filter(r=>`${r.name} ${r.slug} ${r.kind}`.toLowerCase().includes(query.toLowerCase())),[rows,query]);
 function edit(row?:Row){setForm(row?{...row,tags:(row.tags||[]).join(", "),pros:(row.pros||[]).join("\n"),cons:(row.cons||[]).join("\n")}:blank);setMore(Boolean(row?.id));setOpen(true)}
 function quickPrefill(){const url=form.affiliate_url||form.official_url||"";const name=form.name||inferName(url);setForm({...form,name,slug:form.slug||slugify(name),official_url:form.official_url||(()=>{try{return new URL(url).origin}catch{return""}})()});}
 async function save(e:React.FormEvent){e.preventDefault();setMessage("Saving partner…");const payload={...form,slug:form.slug||slugify(form.name),tags:String(form.tags||"").split(/[,\n]/).map((x:string)=>x.trim()).filter(Boolean),pros:String(form.pros||"").split(/[,\n]/).map((x:string)=>x.trim()).filter(Boolean),cons:String(form.cons||"").split(/[,\n]/).map((x:string)=>x.trim()).filter(Boolean)};const r=await fetch("/api/admin/platforms",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const j=await r.json().catch(()=>({}));if(!r.ok||!j.ok){setMessage(j.error||"Save failed.");return}setOpen(false);setMessage("Partner saved successfully.");await load()}
 async function remove(id:string,name:string){if(!confirm(`Delete ${name}?`))return;const r=await fetch(`/api/admin/platforms?id=${encodeURIComponent(id)}`,{method:"DELETE"});const j=await r.json().catch(()=>({}));if(!r.ok||!j.ok){setMessage(j.error||"Delete failed.");return}await load()}
 return <><div className="ax-inventory-toolbar"><div className="ax-inventory-search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search inventory…"/></div><button className="ax-tool-btn" onClick={load}><RefreshCw/>Refresh</button><button className="ax-primary-btn" onClick={()=>edit()}><Plus/>Add partner</button></div>{message&&<div className="ax-admin-alert">{message}</div>}<div className="ax-inventory-grid"><div className="ax-inventory-head"><span>PLATFORM</span><span>TYPE</span><span>STATUS</span><span>PUBLIC</span><span>UPDATED</span><span/></div>{loading?<div className="ax-inventory-empty">Loading inventory…</div>:filtered.length===0?<div className="ax-inventory-empty"><b>No partner records yet.</b><span>Add XM, Eightcap or another approved partner with the “Add partner” button. New records stay hidden until you choose to publish them.</span></div>:filtered.map(row=><div className="ax-inventory-row" key={row.id}><button className="ax-platform-name" onClick={()=>edit(row)}><i>{row.name.slice(0,2).toUpperCase()}</i><span><b>{row.name}</b><small>{row.slug}</small></span></button><span className="ax-type-chip">{row.kind}</span><span className={`ax-state-chip is-${row.status}`}>{row.status}</span><span>{row.visible?"Visible":"Hidden"}</span><span>{row.updated_at?new Date(row.updated_at).toLocaleDateString():"—"}</span><span className="ax-row-actions">{row.official_url&&<a href={row.official_url} target="_blank" rel="noreferrer" aria-label="Open official site"><ExternalLink/></a>}<button onClick={()=>remove(row.id,row.name)} aria-label="Delete"><Trash2/></button></span></div>)}</div>
 {open&&<div className="ax-modal-backdrop" onMouseDown={()=>setOpen(false)}><form className="ax-partner-modal toppick-partner-modal" onSubmit={save} onMouseDown={e=>e.stopPropagation()}><div className="ax-modal-head"><div><span>PARTNER INTAKE</span><h2>{form.id?`Edit ${form.name}`:"Add partner"}</h2><p>Fast editor: add the essentials first. Optional research and SEO fields stay collapsed until you need them.</p></div><button type="button" onClick={()=>setOpen(false)}><X/></button></div><section className="partner-essentials"><div className="partner-quick"><label>Affiliate URL<input value={form.affiliate_url||""} onChange={e=>setForm({...form,affiliate_url:e.target.value})} placeholder="https://partner-link…"/></label><button type="button" className="ax-tool-btn" onClick={quickPrefill}><WandSparkles/>Prefill name</button></div><div className="ax-form-grid"><label>Name *<input required value={form.name||""} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Type<select value={form.kind} onChange={e=>setForm({...form,kind:e.target.value})}><option value="broker">Broker</option><option value="exchange">Exchange</option><option value="wallet">Wallet</option></select></label><label>Slug<input value={form.slug||""} onChange={e=>setForm({...form,slug:e.target.value})} placeholder="auto-from-name"/></label><label>Status<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option value="research">Research</option><option value="verified">Reviewed / eligible</option><option value="restricted">Restricted</option></select></label><label className="wide">Official URL<input value={form.official_url||""} onChange={e=>setForm({...form,official_url:e.target.value})} placeholder="https://official-site…"/></label><label className="wide">Logo URL<input value={form.logo_url||""} onChange={e=>setForm({...form,logo_url:e.target.value})} placeholder="https://…/logo.png"/></label><label className="wide">Short description<textarea value={form.short_description||""} onChange={e=>setForm({...form,short_description:e.target.value})} placeholder="2–3 factual sentences. Avoid guarantees or unsupported claims."/></label></div><div className="ax-toggle-row"><label><input type="checkbox" checked={!!form.featured} onChange={e=>setForm({...form,featured:e.target.checked})}/> Featured near top</label><label><input type="checkbox" checked={!!form.visible} onChange={e=>setForm({...form,visible:e.target.checked})}/> Publicly visible</label></div><div className="partner-safety-note">Safety default: keep “Publicly visible” off until the record, market availability and partner promotion rights are ready. A partner relationship is not treated as proof of legal availability.</div></section><button type="button" className="ax-tool-btn optional-toggle" onClick={()=>setMore(v=>!v)}>{more?"Hide optional details":"+ Optional research & SEO details"}</button>{more&&<section className="ax-form-grid optional-fields"><label>Partner / affiliate ID<input value={form.affiliate_partner_id||""} onChange={e=>setForm({...form,affiliate_partner_id:e.target.value})}/></label><label>Custody model<input value={form.custody_model||""} onChange={e=>setForm({...form,custody_model:e.target.value})}/></label><label className="wide">Tags<input value={form.tags||""} onChange={e=>setForm({...form,tags:e.target.value})} placeholder="forex, mt5, crypto"/></label><label className="wide">Products / account scope<textarea value={form.product_summary||""} onChange={e=>setForm({...form,product_summary:e.target.value})}/></label><label className="wide">Fees / spreads<textarea value={form.fee_summary||""} onChange={e=>setForm({...form,fee_summary:e.target.value})}/></label><label className="wide">Security / custody<textarea value={form.security_summary||""} onChange={e=>setForm({...form,security_summary:e.target.value})}/></label><label className="wide">Regulatory & market context<textarea value={form.regulatory_summary||""} onChange={e=>setForm({...form,regulatory_summary:e.target.value})} placeholder="Entities, regulators, restricted markets, eligibility notes…"/></label><label className="wide">Full review<textarea className="tall" value={form.full_review||""} onChange={e=>setForm({...form,full_review:e.target.value})}/></label><label className="wide">Pros<textarea value={form.pros||""} onChange={e=>setForm({...form,pros:e.target.value})} placeholder="One per line"/></label><label className="wide">Points to consider<textarea value={form.cons||""} onChange={e=>setForm({...form,cons:e.target.value})} placeholder="One per line"/></label><label className="wide">SEO title<input value={form.seo_title||""} onChange={e=>setForm({...form,seo_title:e.target.value})}/></label><label className="wide">SEO description<textarea value={form.seo_description||""} onChange={e=>setForm({...form,seo_description:e.target.value})}/></label></section>}<div className="ax-modal-foot"><button type="button" className="ax-tool-btn" onClick={()=>setOpen(false)}>Cancel</button><button className="ax-primary-btn" type="submit">Save partner</button></div></form></div>}
 </>}
