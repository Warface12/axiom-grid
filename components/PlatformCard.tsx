import Link from "next/link";
import { ArrowUpRight, CheckCircle2, CircleDashed, ShieldAlert } from "lucide-react";
import type { Platform } from "@/lib/types";
export function PlatformCard({platform}:{platform:Platform}){const Icon=platform.status==="verified"?CheckCircle2:platform.status==="restricted"?ShieldAlert:CircleDashed;return <article className="platform-card"><div className="platform-top"><span className={`platform-logo logo-${platform.kind}`}>{platform.logoText}</span><span className={`status-chip status-${platform.status}`}><Icon size={13}/>{platform.status==="research"?"Research pending":platform.status}</span></div><div><span className="kind-label">{platform.kind}</span><h3>{platform.name}</h3><p>{platform.short}</p></div><div className="tag-row">{platform.tags.slice(0,3).map(t=><span key={t}>{t}</span>)}</div><div className="platform-card-foot"><span>Updated {platform.updatedAt}</span><Link href={`/${platform.kind}s/${platform.slug}`}>Research profile <ArrowUpRight size={15}/></Link></div></article>}
