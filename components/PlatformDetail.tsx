import Link from "next/link";
import { ExternalLink } from "lucide-react";
import type { Platform } from "@/lib/types";
export function PlatformDetail({platform}:{platform:Platform}){return <div className="shell content-shell detail-grid"><article className="detail-main"><span className="kind-label">{platform.kind} / RESEARCH PROFILE</span><h1>{platform.name}</h1><p className="page-lead">{platform.description}</p><div className="notice">Research status: this seed profile is not a final endorsement. Current fees, legal entity, availability, product scope, risk disclosures and affiliate eligibility must be verified from official sources before a promotional CTA is enabled.</div><h2>What this profile will evaluate</h2><p>Each final review is designed to separate product facts from editorial judgment. For exchanges that means security controls, fees, funding, available assets, trading tools and transparency. For brokers it also means legal entity, instrument type, leverage/risk disclosures and platform access. Wallet reviews emphasize custody, recovery, supported assets, device/software security and practical usability.</p><h2>Market availability</h2><p>{platform.productNote}</p><h2>Editorial rule</h2><p>A platform can exist in the database without being publicly promoted in every market. Commercial links should remain disabled where market or affiliate eligibility is unknown.</p><div className="tag-row">{platform.tags.map(t=><span key={t}>{t}</span>)}</div></article><aside className="detail-side"><span className={`platform-logo logo-${platform.kind}`}>{platform.logoText}</span><h3>{platform.name}</h3><dl><div><dt>Product type</dt><dd>{platform.kind}</dd></div><div><dt>Research status</dt><dd>{platform.status}</dd></div><div><dt>Custody</dt><dd>{platform.custody||"Not applicable / verify"}</dd></div><div><dt>Market status</dt><dd>{platform.markets.join(", ")}</dd></div><div><dt>Last seed update</dt><dd>{platform.updatedAt}</dd></div></dl><Link className="btn-ghost" href={platform.website} target="_blank" rel="nofollow noopener">Official website <ExternalLink size={15}/></Link></aside></div>}
