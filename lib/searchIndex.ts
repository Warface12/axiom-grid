import {createClient} from "@supabase/supabase-js";import type{Platform}from"@/lib/types";import{visiblePlatformIdsForVisitor}from"@/lib/marketVisibility";
function db(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL;const key=process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return null;return createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}})}
export async function searchPlatforms(q:string,limit=20):Promise<Platform[]>{const s=db();const term=q.trim();if(!s||term.length<2)return[];const{ids}=await visiblePlatformIdsForVisitor();if(!ids.length)return[];const safe=term.replace(/[,%]/g,"");const{data,error}=await s.from("platform").select("*").in("id",ids).eq("visible",true).neq("status","restricted").or(`name.ilike.%${safe}%,short_description.ilike.%${safe}%,product_summary.ilike.%${safe}%`).limit(limit);if(error)return[];return(data||[]).map((row:any)=>({id:row.id,slug:row.slug,name:row.name,short:row.short_description||"Research profile",description:row.full_review||row.short_description||"",kind:row.kind,status:row.status,logoText:String(row.name).slice(0,2).toUpperCase(),logoUrl:row.logo_url||null,tags:Array.isArray(row.tags)?row.tags:[],updatedAt:row.updated_at?new Date(row.updated_at).toISOString().slice(0,10):"Recently",website:row.official_url||"#",affiliateUrl:null,custody:row.custody_model||null,markets:[],productNote:row.regulatory_summary||"",feeSummary:row.fee_summary||null,securitySummary:row.security_summary||null,regulatorySummary:row.regulatory_summary||null,productSummary:row.product_summary||null,pros:Array.isArray(row.pros)?row.pros:[],cons:Array.isArray(row.cons)?row.cons:[],featured:Boolean(row.featured),visible:Boolean(row.visible),seoTitle:row.seo_title||null,seoDescription:row.seo_description||null}))}
